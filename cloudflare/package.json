{
  "name": "readori-source-validator-control",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply readori-source-validator --remote",
    "queue:pull": "wrangler queues consumer http add readori-source-validation"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
